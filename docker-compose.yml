version: "3.9"

services:

  app:
    build: .

    container_name: research-scientist

    ports:
      - "8501:8501"

    volumes:
      - .:/app
      - ./data:/app/data

    env_file:
      - .env

    restart: unless-stopped